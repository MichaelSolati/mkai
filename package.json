{
  "name": "cpx",
  "version": "0.1.0",
  "description": "Claude Profile Loader - manage groups of agents, skills, and commands",
  "type": "module",
  "bin": {
    "cpx": "./dist/cpx.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "node --experimental-vm-modules node_modules/.bin/vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0",
    "chalk": "^5.4.0",
    "commander": "^13.0.0",
    "yaml": "^2.7.0"
  }
}
